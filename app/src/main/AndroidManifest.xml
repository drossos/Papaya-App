<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="papaya.bayviewglen.com.papayatest">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/pap_logo"
        android:label="Papaya"
        android:roundIcon="@mipmap/pap_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- add mobile vision dependencies and version to metadata -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.gms.vision.DEPENDENCIES"
            android:value="barcode" />

        <activity
            android:name=".MainActivity"
            android:label="Papaya">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ScanBarcodeActivity"
            android:label="Scan Barcode" />
        <activity android:name=".InstrumentLogging" />
    </application>

</manifest>